export interface Foods {
  id: number;
  name: string;
  description: string;
  image: string;
  price: string;
  available: boolean;
}

export interface Data {
  name: string;
  description: string;
  image: string;
  price: string;
}